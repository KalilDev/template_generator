name: template_generator
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  build:
  source_gen:
  analyzer:
  path:
  template_annotation:
    path: template_annotation

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
